# 单轮廓内长方体的随机放置

### 生成区间 [a,b] 内的随机整数

```python
import random
print(random.randint(3,303))   
```

### 创建长方形基地轮廓
```python
import matplotlib.pyplot as plt

# 创建3D图形并设置坐标轴范围
fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')
ax.set(xlim=(0, 300), ylim=(0, 300), zlim=(0, 300))

length, width = 240, 200    # 给长方形场地的长度和宽度赋值

site = [[0,0,0],[length,0,0],[length,width,0],[0,width,0]]     # 给场地四个角点坐标赋值（z=0）

# 绘制闭合的场地边界(绿色)
# 对于点A(x1,y1,z1)和点B(x2,y2,z2)的连线,代码表达为ax.plot([x1,x2],[y1,y2],[z1,z2])
for i in range(4):
    start = site[i]
    end = site[(i + 1) % 4]           # 下一个点，最后回到起点
    ax.plot([start[0], end[0]],
            [start[1], end[1]],
            [start[2], end[2]],
            color="green")

plt.show()
```

### 单轮廓内长方体的随机放置

![](/images/三维空间布局算法/随机布局生成算法/底面中心点坐标的随机生成/单轮廓内长方体的随机放置/1a1.jpg)
![](/images/三维空间布局算法/随机布局生成算法/底面中心点坐标的随机生成/单轮廓内长方体的随机放置/1a2.jpg)
![](/images/三维空间布局算法/随机布局生成算法/底面中心点坐标的随机生成/单轮廓内长方体的随机放置/2a1.jpg)
![](/images/三维空间布局算法/随机布局生成算法/底面中心点坐标的随机生成/单轮廓内长方体的随机放置/2a2.jpg)

```python
import matplotlib.pyplot as plt
import random

# ---------------- 3D坐标系初始化 ----------------
fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')
ax.set_xlim((0, 300))
ax.set_ylim((0, 300))
ax.set_zlim((0, 300))

# ---------------- 场地类（二维/三维边界） ----------------
class Site:
    def __init__(self, data_of_points):
        self.data = data_of_points

    def getData(self): 
        return self.data

    def edges(self): 
        return len(self.data)

    # ---------------- 绘制方法 ----------------
    def render(self):
        data = self.data

        # ---------------- 闭合多边形绘制 ----------------
        for i in range(len(data)):
            start = data[i]
            end = data[(i + 1) % len(data)]  # 自动闭合

            ax.plot([start[0], end[0]],
                    [start[1], end[1]],
                    [start[2], end[2]],
                    color="green")

# ---------------- 场地定义 ----------------
length = 240
width = 200

site = [
    [0, 0, 0],
    [length, 0, 0],
    [length, width, 0],
    [0, width, 0]
]

offset = 10

site_of_property_line = [
    [0 + offset, 0 + offset, 0],
    [length - offset, 0 + offset, 0],
    [length - offset, width - offset, 0],
    [0 + offset, width - offset, 0]
]

# ---------------- 绘制场地 ----------------
s = Site(site)
s.render()

s1 = Site(site_of_property_line)
s1.render()

# ---------------- 长方体类 ----------------
class Cuboid:
    def __init__(self, x, y, z, length, width, height):
        self.x = x
        self.y = y
        self.z = z        
        self.length = length
        self.width = width   
        self.height = height

    def getx(self): return self.x
    def gety(self): return self.y
    def getz(self): return self.z    
    def getlength(self): return self.length
    def getwidth(self): return self.width
    def getheight(self): return self.height

    def areaInBase(self):
        return self.length * self.width

    def volume(self):
        return self.length * self.width * self.height        

    def coordinates_of_base_center(self):
        x = self.x + self.length / 2
        y = self.y + self.width / 2
        z = self.z
        return [x, y, z]

    # ---------------- 绘制方法 ----------------
    def render(self):
        ax.bar3d(self.x, self.y, self.z,
                 self.length, self.width, self.height,
                 color="green", alpha=0.5)

# ---------------- 随机生成一个立方体 ----------------
length_of_cuboid = 80
width_of_cuboid = 60
height_of_cuboid = 100

x_of_cuboid = random.randint(0 + offset, length - offset - length_of_cuboid)
y_of_cuboid = random.randint(0 + offset, width - offset - width_of_cuboid)
z_of_cuboid = 0

cuboid1 = Cuboid(x_of_cuboid, y_of_cuboid, z_of_cuboid,
                 length_of_cuboid, width_of_cuboid, height_of_cuboid)

cuboid1.render()

# ---------------- 显示结果 ----------------
plt.show()
```

### 单轮廓内两个长方体的随机放置
```python
import matplotlib.pyplot as plt
import random

# ---------------- 3D坐标系初始化 ----------------
fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')
ax.set(xlim=(0, 300), ylim=(0, 300), zlim=(0, 300))

# ---------------- 场地类 ----------------
class Site:
    def __init__(self, data_of_points):
        self.data = data_of_points

    def getData(self): 
        return self.data

    def edges(self): 
        return len(self.data)

    # ---------------- 绘制方法（闭合多边形） ----------------
    def render(self):
        data = self.data

        # ---------------- 场地闭合绘制 ----------------
        for i in range(len(data)):
            start = data[i]
            end = data[(i + 1) % len(data)]  # 自动闭合

            ax.plot([start[0], end[0]],
                    [start[1], end[1]],
                    [start[2], end[2]],
                    color="green")

# ---------------- 场地数据 ----------------
length = 240
width = 200

site = [
    [0, 0, 0],
    [length, 0, 0],
    [length, width, 0],
    [0, width, 0]
]

offset = 10

site_of_property_line = [
    [0 + offset, 0 + offset, 0],
    [length - offset, 0 + offset, 0],
    [length - offset, width - offset, 0],
    [0 + offset, width - offset, 0]
]

# ---------------- 绘制场地 ----------------
s = Site(site)
s.render()

s1 = Site(site_of_property_line)
s1.render()

# ---------------- 长方体类 ----------------
class Cuboid:
    def __init__(self, x, y, z, length, width, height):
        self.x = x
        self.y = y
        self.z = z        
        self.length = length
        self.width = width   
        self.height = height

    def getx(self): return self.x
    def gety(self): return self.y
    def getz(self): return self.z    
    def getlength(self): return self.length
    def getwidth(self): return self.width
    def getheight(self): return self.height

    def areaInBase(self):
        return self.length * self.width

    def volume(self):
        return self.length * self.width * self.height        

    def coordinates_of_base_center(self):
        x = self.x + self.length / 2
        y = self.y + self.width / 2
        z = self.z
        return [x, y, z]

    # ---------------- 绘制方法 ----------------
    def render(self):
        ax.bar3d(self.x, self.y, self.z,
                 self.length, self.width, self.height,
                 color="green", alpha=0.5)

# ---------------- 参数定义 ----------------
length_of_cuboid = 80
width_of_cuboid = 60
height_of_cuboid = 100

# ---------------- 随机生成Cuboid 1 ----------------
x_of_cuboid = random.randint(0 + offset, length - offset - length_of_cuboid)
y_of_cuboid = random.randint(0 + offset, width - offset - width_of_cuboid)
z_of_cuboid = 0

cuboid1 = Cuboid(x_of_cuboid, y_of_cuboid, z_of_cuboid,
                 length_of_cuboid, width_of_cuboid, height_of_cuboid)
cuboid1.render()

# ---------------- 随机生成Cuboid 2 ----------------
x_of_cuboid = random.randint(0 + offset, length - offset - length_of_cuboid)
y_of_cuboid = random.randint(0 + offset, width - offset - width_of_cuboid)
z_of_cuboid = 0

cuboid2 = Cuboid(x_of_cuboid, y_of_cuboid, z_of_cuboid,
                 length_of_cuboid, width_of_cuboid, height_of_cuboid)
cuboid2.render()

# ---------------- 显示结果 ----------------
plt.show()
```

## 参考文献及资料

1. [random — Generate pseudo-random numbers](https://docs.python.org/3/library/random.html#module-random)
2. [Python random 模块](https://www.runoob.com/python3/python-random.html)
3. [Python random() 函数](https://www.runoob.com/python/func-number-random.html)
