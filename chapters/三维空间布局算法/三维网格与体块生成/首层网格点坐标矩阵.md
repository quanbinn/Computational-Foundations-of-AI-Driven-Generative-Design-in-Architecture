# 首层网格点坐标矩阵

## 开始做实体实验

![](/images/三维空间布局算法/三维网格与体块生成/把网格细分后的多个位置点的坐标写入矩阵/1a1.jpg)

### 将多个坐标点写入矩阵

```python
import matplotlib.pyplot as plt

# 创建3D绘图窗口和坐标轴
ax = plt.figure().add_subplot(111, projection='3d')

# 设置坐标轴范围
ax.set_xlim((0, 300))
ax.set_ylim((0, 300))
ax.set_zlim((0, 300))

# 定义矩形基地的长宽（z方向为0，表示平面）
length = 240
width = 200
site = [
    [0, 0, 0],          # 左下角
    [length, 0, 0],     # 右下角
    [length, width, 0], # 右上角
    [0, width, 0]       # 左上角
]  # print(site)  # 保留硬编码注释，用于调试或检查坐标

# 绘制矩形基地的四条边
ax.plot([site[0][0], site[1][0]], [site[0][1], site[1][1]], [site[0][2], site[1][2]], color="green")  
ax.plot([site[1][0], site[2][0]], [site[1][1], site[2][1]], [site[1][2], site[2][2]], color="green")
ax.plot([site[2][0], site[3][0]], [site[2][1], site[3][1]], [site[2][2], site[3][2]], color="green")
ax.plot([site[3][0], site[0][0]], [site[3][1], site[0][1]], [site[3][2], site[0][2]], color="green")

# -----------------------------
# 设置长边和短边的等距采样间距
distance_in_length = 16
distance_in_width = 16

# 计算沿长边和短边可分的段数
segments_in_length = int(length / distance_in_length)        # print(segments_in_length)
segments_in_width  = int(width / distance_in_width)          # print(segments_in_width)

# -----------------------------
# 生成沿长边的等距采样点
xArrayInLength = []
for i in range(1, segments_in_length):
    xArrayInLength.append(distance_in_length * i)

# 生成沿短边的等距采样点
yArrayInWidth = []
for i in range(1, segments_in_width):
    yArrayInWidth.append(distance_in_width * i)

# -----------------------------
# 初始化二维列表，用于存储网格交点坐标
intersections = []
for l in range(1, segments_in_width):
    intersections.append([])  # 每一行对应一个y方向的采样线

print(intersections)  # 打印空列表结构，用于教学查看初始化

# -----------------------------
# 设置楼层高度（z坐标）
numberOfFloor = 1

# 生成所有交点坐标并存入intersections矩阵
for j in range(0, segments_in_width-1):
    for i in range(1, segments_in_length):
        intersections[j].append([xArrayInLength[i-1], yArrayInWidth[j], numberOfFloor])

print(intersections)  # 打印生成的交点坐标矩阵，用于教学查看

# -----------------------------
# 保留硬编码示例注释（原始逐行赋值方式）
# for i in range(1,segments_in_length):
#     intersections[0].append([xArrayInLength[i-1],yArrayInWidth[0],numberOfFloor])
# for i in range(1,segments_in_length):
#     intersections[1].append([xArrayInLength[i-1],yArrayInWidth[1],numberOfFloor])  
# for i in range(1,segments_in_length):
#     intersections[2].append([xArrayInLength[i-1],yArrayInWidth[2],numberOfFloor])  
# for i in range(1,segments_in_length):
#     intersections[3].append([xArrayInLength[i-1],yArrayInWidth[3],numberOfFloor])
```

### 打印多个点坐标

```python
import matplotlib.pyplot as plt

# -----------------------------
# 创建3D绘图窗口和坐标轴
ax = plt.figure().add_subplot(111, projection='3d')

# 设置坐标轴范围
ax.set_xlim((0, 300))
ax.set_ylim((0, 300))
ax.set_zlim((0, 300))

# -----------------------------
# 定义矩形基地的长宽（z方向为0，表示平面）
length = 240
width = 200
site = [
    [0, 0, 0],          # 左下角
    [length, 0, 0],     # 右下角
    [length, width, 0], # 右上角
    [0, width, 0]       # 左上角
]  # print(site)  # 保留硬编码注释，用于检查坐标

# 绘制矩形基地的四条边
ax.plot([site[0][0], site[1][0]], [site[0][1], site[1][1]], [site[0][2], site[1][2]], color="green")  
ax.plot([site[1][0], site[2][0]], [site[1][1], site[2][1]], [site[1][2], site[2][2]], color="green")
ax.plot([site[2][0], site[3][0]], [site[2][1], site[3][1]], [site[2][2], site[3][2]], color="green")
ax.plot([site[3][0], site[0][0]], [site[3][1], site[0][1]], [site[3][2], site[0][2]], color="green")

# -----------------------------
# 设置长边和短边的等距采样间距
distance_in_length = 16
distance_in_width = 16

# 计算沿长边和短边可分的段数
segments_in_length = int(length / distance_in_length)  # print(segments_in_length)
segments_in_width  = int(width / distance_in_width)    # print(segments_in_width)

# -----------------------------
# 生成沿长边的等距采样点
xArrayInLength = []
for i in range(1, segments_in_length):
    xArrayInLength.append(distance_in_length * i)

# 生成沿短边的等距采样点
yArrayInWidth = []
for i in range(1, segments_in_width):
    yArrayInWidth.append(distance_in_width * i)

# -----------------------------
# 初始化二维列表，用于存储网格交点坐标
intersections = []
for l in range(1, segments_in_width):
    intersections.append([])  # 每一行对应一个y方向的采样线

print(intersections)  # 打印空列表结构，用于教学查看初始化

# -----------------------------
# 设置楼层高度（z坐标）
numberOfFloor = 0

# 生成所有交点坐标并存入intersections矩阵
for j in range(0, segments_in_width-1):
    for i in range(1, segments_in_length):
        intersections[j].append([xArrayInLength[i-1], yArrayInWidth[j], numberOfFloor])

print(intersections)  # 打印生成的交点坐标矩阵，用于教学查看
print(len(intersections))  # 打印行数，用于教学理解矩阵结构

# -----------------------------
# 绘制所有交点
for j in range(0, len(intersections)):
    for i in range(0, len(intersections[j])):
        ax.scatter(
            intersections[j][i][0],  # x坐标
            intersections[j][i][1],  # y坐标
            intersections[j][i][2],  # z坐标
            c="red"                  # 点颜色为红色
        )

# -----------------------------
# 显示3D图形
plt.show()
```