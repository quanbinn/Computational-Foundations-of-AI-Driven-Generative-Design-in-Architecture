# 大矩形区域内小矩形的随机生成

### 生成区间 [a,b] 内的随机整数
```python
import random
print(random.randint(3,303))   
```

### 给定大矩形域内小矩形的随机放置

![](/images/二维空间布局算法/随机布局生成算法/随机生成矩形中心点坐标/大矩形区域内小矩形的随机生成/1a1.jpg)
![](/images/二维空间布局算法/随机布局生成算法/随机生成矩形中心点坐标/大矩形区域内小矩形的随机生成/1a2.jpg)
![](/images/二维空间布局算法/随机布局生成算法/随机生成矩形中心点坐标/大矩形区域内小矩形的随机生成/2a1.jpg)
![](/images/二维空间布局算法/随机布局生成算法/随机生成矩形中心点坐标/大矩形区域内小矩形的随机生成/2a2.jpg)

```python
import matplotlib.pyplot as plt
import random

# ---------------- 基础绘图设置 ----------------
plt.axis('equal')

# ---------------- 几何对象：矩形类 ----------------
class Rectangle:
    def __init__(self, x, y, width, height):
        # 矩形左下角坐标 + 宽高（教学硬编码结构）
        self.x = x
        self.y = y
        self.width = width
        self.height = height        
    
    # ---------------- 基础属性获取 ----------------
    def getX(self): return self.x
    def getY(self): return self.y
    def getWidth(self): return self.width
    def getHeight(self): return self.height

    # ---------------- 面积计算 ----------------
    def area(self):
        area = self.width * self.height
        return area

    # ---------------- 中心点计算 ----------------
    def centralCoordinates(self):
        x_of_center = self.x + self.width / 2
        y_of_center = self.y + self.height / 2
        return [x_of_center, y_of_center]

    # ---------------- 绘制方法 ----------------
    def render(self):
        p1 = [self.x, self.y]
        p2 = [self.x + self.width, self.y] 
        p3 = [self.x + self.width, self.y + self.height]
        p4 = [self.x, self.y + self.height]

        plt.plot([p1[0], p2[0]], [p1[1], p2[1]], color="green")
        plt.plot([p2[0], p3[0]], [p2[1], p3[1]], color="green")
        plt.plot([p3[0], p4[0]], [p3[1], p4[1]], color="green")
        plt.plot([p4[0], p1[0]], [p4[1], p1[1]], color="green")

# ---------------- Site（场地）定义 ----------------
x_of_site = 36 
y_of_site = 78
width_of_site = 250
height_of_site = 200

rect_of_site = Rectangle(x_of_site, y_of_site, width_of_site, height_of_site)
rect_of_site.render()

# ---------------- 退界线（property line） ----------------
property_line_offset = 4

x_of_property_line_rec = x_of_site + property_line_offset
y_of_property_line_rec = y_of_site + property_line_offset
width_of_property_line_rec = width_of_site - property_line_offset * 2
height_of_property_line_rec = height_of_site - property_line_offset * 2

rect_of_property_line_rec = Rectangle(
    x_of_property_line_rec,
    y_of_property_line_rec,
    width_of_property_line_rec,
    height_of_property_line_rec
)
rect_of_property_line_rec.render()

# ---------------- 随机生成建筑矩形 ----------------
width_of_rec = 20
height_of_rec = 24

x_of_rec = random.randint(
    x_of_property_line_rec,
    x_of_property_line_rec + width_of_property_line_rec - width_of_rec
)

y_of_rec = random.randint(
    y_of_property_line_rec,
    y_of_property_line_rec + height_of_property_line_rec - height_of_rec
)

rect1 = Rectangle(x_of_rec, y_of_rec, width_of_rec, height_of_rec)
rect1.render()

# ---------------- 显示结果 ----------------
plt.show()
```

### 给定大矩形域内两个小矩形的随机放置

```python
import matplotlib.pyplot as plt
import random

# ---------------- 基础绘图设置 ----------------
plt.axis('equal')

# ---------------- 几何对象：矩形类 ----------------
class Rectangle:
    def __init__(self, x, y, width, height):
        # 矩形左下角坐标 + 宽高（教学硬编码结构）
        self.x = x
        self.y = y
        self.width = width
        self.height = height        
    
    # ---------------- 基础属性获取 ----------------
    def getX(self): return self.x
    def getY(self): return self.y
    def getWidth(self): return self.width
    def getHeight(self): return self.height

    # ---------------- 面积计算 ----------------
    def area(self):
        area = self.width * self.height
        return area

    # ---------------- 中心点计算 ----------------
    def centralCoordinates(self):
        xOfCenter = self.x + self.width / 2
        yOfCenter = self.y + self.height / 2
        return [xOfCenter, yOfCenter]

    # ---------------- 绘制方法 ----------------
    def render(self):
        p1 = [self.x, self.y]
        p2 = [self.x + self.width, self.y] 
        p3 = [self.x + self.width, self.y + self.height]
        p4 = [self.x, self.y + self.height]

        plt.plot([p1[0], p2[0]], [p1[1], p2[1]], color="green")
        plt.plot([p2[0], p3[0]], [p2[1], p3[1]], color="green")
        plt.plot([p3[0], p4[0]], [p3[1], p4[1]], color="green")
        plt.plot([p4[0], p1[0]], [p4[1], p1[1]], color="green")

# ---------------- Site（场地）定义 ----------------
x_of_site = 36 
y_of_site = 78
width_of_site = 250
height_of_site = 200

rect_of_site = Rectangle(x_of_site, y_of_site, width_of_site, height_of_site)
rect_of_site.render()

# ---------------- 退界线（property line） ----------------
property_line_offset = 4

x_of_property_line_rec = x_of_site + property_line_offset
y_of_property_line_rec = y_of_site + property_line_offset
width_of_property_line_rec = width_of_site - property_line_offset * 2
height_of_property_line_rec = height_of_site - property_line_offset * 2

rect_of_property_line_rec = Rectangle(
    x_of_property_line_rec,
    y_of_property_line_rec,
    width_of_property_line_rec,
    height_of_property_line_rec
)
rect_of_property_line_rec.render()

# ---------------- 随机生成建筑矩形 ----------------
width_of_rec = 20
height_of_rec = 24

# ---------------- 生成第1个随机矩形 ----------------
x_of_rec = random.randint(
    x_of_property_line_rec,
    x_of_property_line_rec + width_of_property_line_rec - width_of_rec
)
y_of_rec = random.randint(
    y_of_property_line_rec,
    y_of_property_line_rec + height_of_property_line_rec - height_of_rec
)

rect1 = Rectangle(x_of_rec, y_of_rec, width_of_rec, height_of_rec)
rect1.render()

# ---------------- 生成第2个随机矩形 ----------------
x_of_rec = random.randint(
    x_of_property_line_rec,
    x_of_property_line_rec + width_of_property_line_rec - width_of_rec
)
y_of_rec = random.randint(
    y_of_property_line_rec,
    y_of_property_line_rec + height_of_property_line_rec - height_of_rec
)

rect2 = Rectangle(x_of_rec, y_of_rec, width_of_rec, height_of_rec)
rect2.render()

# ---------------- 显示结果 ----------------
plt.show()
```

## 参考文献及资料

1. [random — Generate pseudo-random numbers](https://docs.python.org/3/library/random.html#module-random)
2. [Python random 模块](https://www.runoob.com/python3/python-random.html)
3. [Python random() 函数](https://www.runoob.com/python/func-number-random.html)
